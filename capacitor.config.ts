import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.guguild',
  appName: 'GU-guild',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
